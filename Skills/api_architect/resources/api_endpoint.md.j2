{% for item in items %}
## {{ item.name }}

<div class="api-method-banner">
  <span class="api-method method-{{ item.method | lower }}">{{ item.method }}</span> <span class="api-path">{{ item.url_path }}</span>
</div>

{{ item.description | replace('**Note:**', '> [!NOTE]\n>') | replace('You must use a [JWT]', '> [!IMPORTANT]\n> You must use a [JWT]') }}

{% if item.parameters %}
### Parameters

| Parameter | Type | Required | Description | Example |
| --- | --- | --- | --- | --- |
{% for param in item.parameters %}| `{{ param.name }}` | `{{ param.type }}` | {{ "Yes" if param.required else "No" }} | {{ param.description }} | `{{ param.example }}` |
{% endfor %}
{% endif %}

{% if item.request_body %}
### Request Body

```json
{{ item.request_body }}
```
{% endif %}

{% if item.snippets %}
### Request Examples

=== "cURL"

    ```bash
{{ item.snippets.curl | indent(4, True) }}
    ```

=== "Python (Requests)"

    ```python
{{ item.snippets.python | indent(4, True) }}
    ```

=== "JavaScript (Octokit)"

    ```javascript
{{ item.snippets.javascript | indent(4, True) }}
    ```
{% endif %}

### Response Example

```json
{{ item.response_body }}
```

{% if item.notes %}
> [!NOTE]
> {{ item.notes }}
{% endif %}

---

{% endfor %}
