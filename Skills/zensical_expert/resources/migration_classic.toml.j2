[project]
site_name    = "{{ site_name | default('My Site') }}"
site_url     = "{{ site_url | default('https://example.com/') }}"
docs_dir     = "docs"
site_dir     = "site"

# Classic theme = identical appearance to Material for MkDocs
[project.theme]
variant = "classic"
features = [
    "navigation.instant",
    "navigation.tabs",
    "navigation.sections",
    "navigation.top",
    "toc.follow",
    "search.suggest",
    "search.highlight",
    "content.code.copy",
    "content.tabs.link",
]
palette = [
    {scheme = "default", primary = "teal", accent = "teal",
     toggle = {icon = "material/brightness-7", name = "Dark mode"}},
    {scheme = "slate",   primary = "teal", accent = "teal",
     toggle = {icon = "material/brightness-4", name = "Light mode"}}
]

# Extensions — match your existing mkdocs.yml markdown_extensions list
[project.markdown_extensions.admonition]
[project.markdown_extensions.pymdownx.details]
[project.markdown_extensions.pymdownx.superfences]
[project.markdown_extensions.pymdownx.highlight]
  anchor_linenums = true
[project.markdown_extensions.pymdownx.tabbed]
  alternate_style = true
[project.markdown_extensions.pymdownx.tasklist]
  custom_checkbox = true
[project.markdown_extensions.pymdownx.emoji]
  emoji_index     = "material"
  emoji_generator = "material"
[project.markdown_extensions.attr_list]
[project.markdown_extensions.def_list]
[project.markdown_extensions.footnotes]
[project.markdown_extensions.tables]
[project.markdown_extensions.toc]
  permalink = true

[project.plugins.search]
